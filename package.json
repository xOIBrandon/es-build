{
  "name": "es-build",
  "version": "1.0.0",
  "description": "es build test repo",
  "main": "build/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=build/index.js --target=es2017 --format=esm --tree-shaking=false"
  },
  "author": "Branson",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.23"
  },
  "dependencies": {
    "lodash.capitalize": "^4.2.1"
  }
}
